import 'package:flutter/material.dart';

class PaginaDashBoard extends StatelessWidget {
  const PaginaDashBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla de Las dashBoasrds'),
    );
  }
}

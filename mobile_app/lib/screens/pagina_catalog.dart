import 'package:flutter/material.dart';

class PaginaCatalog extends StatelessWidget {
  const PaginaCatalog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla del Catalogo'),
    );
  }
}

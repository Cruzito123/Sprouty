import 'package:flutter/material.dart';
import 'tema_app.dart';

// ⚠️ Mantenemos temporalmente estas constantes antiguas, 
// pero apuntando a AppColors para no romper la app entera de inmediato.
// IMPORTANTE: El objetivo es ir reemplazándolas en el código por 
// Theme.of(context).cardColor o similar.

const kPrimario = AppColors.primario;
const kFondoCrema = AppColors.fondoClaro;
const kDivisor = AppColors.secundiario;
